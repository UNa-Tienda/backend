## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5
spring.jpa.generate-ddl=false

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/una_tienda
spring.datasource.username=administration
spring.datasource.password=rootingesoft

# spring.jpa.properties.hibernate.default_schema=techwriting
spring.datasource.hikari.schema=public

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create

#spring.datasource.initialization-mode=always
#spring.datasource.platform=postgres

# spring.security.user.name=admin
# spring.security.user.password=admin
server.port=8080
